#include <stdio.h>

/*
int main()
{
    int a = 45345;
    int x = 435;
    int e = a + x;
    // int b = 4534.5; // Not recomended because it is a float value
    float b = 4534.5; // Right way to assign a value
    char c = 'A';     // Right way to assign a character

    printf("The value of a is %d and x is %d \n", a, x); // %d for integers
    printf("The value of b is %f \n", b);                // %f for real number
    printf("The value of b is %c \n", c);                // %c for characters
    printf("The value of a+x is %d \n", a + x);          // We can add, subtraction, muntiplication and many more
    printf("The value of e=(a+x) is %d \n", e);

    // printf("Hello world");
    return 0;
}
*/





// Created a program that can add two numbers
int main() {
    int a = 5;
    int b = 465;

    printf("Value of a = %d and \nValue of b = %d \nTotal value is %d", a, b, a+b);
}